/**
 * @public
 */
export enum eGateStateEnum {
  noMessage,
  login,
  questInformation,
  questCompleted,
}

/**
 * @public
 */
export enum eGateTokenType {
  token,
  wearable,
  quest,
}

/**
 * @public
 */
export enum eCurrency {
  none,
  mana,
  eth,
  matic,
  weth,
  boson,
  usdc,
  usdt,
  dai,
  usd,
}
